# Local-only example. Never commit real secrets.

# Live odds ingestion. In scheduled production this belongs in GitHub Actions secrets.
ODDS_API_KEY=

# Optional local path overrides for bounded development only.
NFL_EDGE_DATA_DIR=data
NFL_EDGE_ARTIFACT_DIR=artifacts
NFL_EDGE_PUBLIC_DIR=site/public
